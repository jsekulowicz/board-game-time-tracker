patch:
  summary: End the current move for a specific player in a game session
  operationId: endPlayerMove
  parameters:
    - name: sessionUuid
      in: path
      required: true
      schema:
        $ref: ../components/schemas/common.yaml#/schemas/Uuid
  requestBody:
    required: true
    description: The player whose current move should be ended
    content:
      application/json:
        schema:
          type: object
          properties:
            playerUuid:
              $ref: ../components/schemas/common.yaml#/schemas/Uuid
          required: [playerUuid]
  responses:
    '200':
      description: The game session after ending the player's move
      content:
        application/json:
          schema:
            $ref: ../components/schemas/game-session.yaml#/schemas/Resource
    '404':
      $ref: ../components/responses.yaml#/responses/Error404
    '500':
      $ref: ../components/responses.yaml#/responses/Error500
